export type Group = {
    id?: number,
    name: string,
    description: string
}

export type Member = {
    id?: number,
    name: string,
    nickname: string,
    phone: string,
    address: string,
    note: string
}